# Environment Variables for WanderLust Application

# Node Environment
NODE_ENV=production

# Server Port (automatically set by hosting platforms)
PORT=8080

# MongoDB Atlas Database URL
# Replace with your MongoDB Atlas connection string
ATLASDB_URL=mongodb+srv://<username>:<password>@cluster.mongodb.net/wanderlust?retryWrites=true&w=majority

# Cloudinary Configuration
# Get these from your Cloudinary dashboard
CLOUD_NAME=your_cloud_name
CLOUD_API_KEY=your_api_key
CLOUD_API_SECRET=your_api_secret

# Session Secret
# Use a strong, random string for production
SESSION_SECRET=your_super_secret_session_key_here

# Instructions:
# 1. Copy this file to .env
# 2. Replace all placeholder values with your actual credentials
# 3. Never commit .env file to version control
# 4. For deployment, set these variables in your hosting platform's environment settings